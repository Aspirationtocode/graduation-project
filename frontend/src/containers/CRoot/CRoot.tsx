import * as React from "react";

import * as classNames from "classnames/bind";
const css = classNames.bind(require("./CRoot.styl"));

// https://material.io/tools/icons/?style=outline

export class CRoot extends React.Component {
  render() {
    return null;
  }
}
